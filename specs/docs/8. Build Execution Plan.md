# Wireless Connect Online Store
# Build Execution Plan

Version: v3.0 — Updated June 2026 to reflect completed implementation

Companion Documents:

- Product Requirements Document (PRD)
- UX & Design Guidelines
- Database Schema Specification
- Technical Architecture Document
- User Flows Document
- Admin Dashboard Requirements
- Content & Inventory Strategy
- Localization & Multilingual Strategy
- API & Server Actions Specification
- Component Library Specification
- Email Templates Specification
- QA & Testing Plan

---

# 1. Purpose

This document serves as the master implementation roadmap for the Wireless Connect Online Store.

It breaks development into manageable sprints and milestones that Claude Code, Codex, or human developers can execute incrementally.

The objective is to:

- Launch a production-ready MVP quickly
- Reduce implementation risk
- Enable testing after each milestone
- Prevent overengineering
- Create a foundation for future expansion

---

# 2. Project Success Criteria

The MVP is considered successful when:

- Customers can browse products
- Customers can search and filter products
- Customers can purchase products online
- Customers can use the storefront in English or Spanish
- Customers can select local pickup or shipping
- Staff can manage inventory
- Staff can manage orders
- Stripe payments work reliably
- Product inventory updates correctly
- The website is mobile-friendly
- The site is production deployed

---

# 3. Development Methodology

## Principles

1. Build foundation before features
2. Build storefront before admin tools
3. Build catalog before checkout
4. Build checkout before reporting
5. Ship working functionality frequently
6. Maintain deployable code after every sprint

## Workflow

```text
Plan
→ Build
→ Test
→ Review
→ Deploy
→ Next Sprint
```

---

# 4. Sprint Overview

| Sprint | Name | Status |
|----------|----------|----------|
| Sprint 0 | Project Setup | ✅ Complete |
| Sprint 1 | Foundation & Design System | ✅ Complete |
| Sprint 2 | Product Catalog | ✅ Complete |
| Sprint 3 | Cart | ✅ Complete |
| Sprint 4 | Checkout & Payments | ✅ Complete |
| Sprint 5 | Admin Dashboard | ✅ Complete |
| Sprint 6 | Launch Readiness | ✅ Complete |
| Sprint 7 | Post-MVP Features | ✅ Complete |
| Sprint 8 | Operations & Tooling | ✅ Complete |

---

# Sprint 0 — Project Setup

## Goal

Create a stable development foundation.

## Deliverables

### Repository Setup

- Create GitHub repository
- Configure branch strategy
- Configure pull request workflow

### Next.js Setup

- Next.js 15
- TypeScript
- App Router
- ESLint
- Prettier

### Styling

- Tailwind CSS
- shadcn/ui

### Hosting

- Vercel project
- Preview deployments

### Supabase

- Create Supabase project
- Configure environments
- Configure local development

### Environment Variables

Configure:

```env
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_TAX_ENABLED=true

RESEND_API_KEY=
NEXT_PUBLIC_DEFAULT_LOCALE=en
NEXT_PUBLIC_SUPPORTED_LOCALES=en,es

SELLER_ID_ENCRYPTION_KEY=

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
```

### Internationalization Foundation

Configure:

- next-intl
- Locale-aware routing structure
- Translation message folders
- Locale middleware strategy

Supported MVP locales:

```text
en
es
```

## Definition of Done

- Project runs locally
- Preview deployment works
- TypeScript builds successfully
- Tailwind works
- Supabase connection verified
- Localization infrastructure configured
- All environment variables documented
- Supabase Auth SMTP configured for Resend routing
- HTTP security headers configured in `next.config.js`

---

# Sprint 1 — Foundation & Design System

## Goal

Build reusable UI foundation.

## Deliverables

### Layouts

- Root layout
- Storefront layout
- Admin layout
- Locale layout

### Navigation

- Header
- Footer
- Mobile menu
- Search trigger
- Cart icon
- Language switcher

### Design System Components

Create:

```text
Button
Input
Textarea
Select
Checkbox
RadioGroup
Badge
Card
Dialog
Drawer
Toast
```

### Store Components

Create:

```text
ProductCard
ConditionBadge
TrustBadge
BatteryHealthIndicator
PriceDisplay
EmptyState
LoadingSkeleton
PageHeader
LanguageSwitcher
LocalizedTooltip
```

### Authentication

- Sign In page
- Sign Up page
- Auth callback route
- Session handling

### Localization

- Install next-intl
- Configure locale-aware routing
- Configure translation message files
- Create language switcher
- Create locale cookie handling
- Configure English fallback behavior

## Definition of Done

- Navigation complete
- Responsive layouts complete
- Authentication working
- Component library established
- Language switching works
- English and Spanish routes work

---

# Sprint 2 — Product Catalog

## Goal

Allow customers to browse products.

## Deliverables

### Database

Deploy:

```text
categories
products (with supported_bands, network_compatibility fields)
product_images
device_intakes (with seller identity fields, hold_until_date)
device_intake_images
public_products view
customer_order_items view
```

### Product Pages

Create:

```text
/
/shop
/shop/[category]
/products/[slug]
```

### Features

- Product listing
- Product detail page
- Product gallery
- Related products

### Search

Implement:

- Product search
- Search overlay
- Search suggestions

### Filters

Implement:

- Brand
- Storage
- Carrier
- Condition
- Price

### Sorting

Implement:

```text
Featured
Newest
Price Low → High
Price High → Low
```

### Localization
- Translate storefront
- Translate navigation
- Translate search/filter labels
- Localized product content resolution
- Localized category content resolution
- Localized metadata generation
- English fallback behavior

## Definition of Done

- Products load from database
- Search works
- Filters work
- Product detail page complete
- Mobile responsive
- Product content resolves correctly by locale
- Device intake schema deployed

---

# Sprint 3 — Cart

## Goal

Allow customers to manage a shopping cart.

## Deliverables

### Database

Deploy:

```text
carts
cart_items
```

### Features

- Add to cart
- Remove from cart
- Update quantity
- Cart drawer
- Cart page

### Guest Cart

Implement:

```text
wc_cart_id cookie
wc_locale cookie
```

### Authenticated Cart

Implement:

- User carts
- Cart merge after login

### Validation

Verify:

- Product exists
- Product active
- Inventory available

## Definition of Done

- Guest carts work
- Logged-in carts work
- Cart persists
- Quantity updates work
- Locale persists during cart usage

---

# Sprint 4 — Checkout & Payments

## Goal

Allow customers to purchase products.

## Deliverables

### Database

Deploy:

```text
addresses
orders (with guest_access_token, tracking_number, shipping_carrier, shipping_insurance_amount, stripe_tax fields)
order_items
warranties (with claim_submitted_at, claim_description)
warranty_claim_images
webhook_events
order_cancellation_requests
data_deletion_requests
```

### Checkout

Build:

```text
/checkout
```

Sections:

- Contact
- Fulfillment
- Shipping
- Review
- Customer locale persistence

### Stripe

Implement:

- Checkout Sessions with `automatic_tax: { enabled: true }` (required for nationwide selling)
- Stripe redirect
- Success flow

### Webhooks

Implement with idempotency:

```text
checkout.session.completed
payment_intent.succeeded
payment_intent.payment_failed
charge.refunded
```

- `webhook_events` table check before processing (ON CONFLICT DO NOTHING)
- Warranty record creation
- Warranty creation from order item snapshots
- Atomic inventory decrement

### Inventory

Implement:

- Inventory validation
- Server-side inventory validation before Stripe Checkout
- Server-side inventory validation after Stripe payment confirmation
- Inventory decrement
- Publishing gate validation (15 canonical gates, including 6 customer-facing images)
- Last-unit inventory validation

### Email

Implement:

- Localized order confirmation (with guest_access_token URL for guests)
- Localized admin new order notification
- Localized order cancellation email

### Order Confirmation

Create:

```text
/order-confirmation?token=[guest_access_token]
```

Guest order lookup uses `guest_access_token` — not order number or order ID.

### Customer Requests

Implement:

- Customer cancellation request action for eligible paid/processing orders
- Guest cancellation request via `guest_access_token`
- CCPA data deletion request action backed by `data_deletion_requests`

### Localization
- Localized checkout
- Localized order confirmation
- Store customer locale on orders
- Localized email delivery

## Definition of Done

- Orders created with `guest_access_token`
- Stripe payments work with Stripe Tax enabled
- Inventory updates correctly using atomic SQL
- Webhook idempotency enforced via `webhook_events` table
- Confirmation email sent with guest token URL
- Customer locale stored on orders
- Customer emails use correct locale
- Warranty records created for eligible orders
- Tax jurisdiction stored on orders

---

# Sprint 5 — Admin Dashboard

## Goal

Allow staff to manage products and orders.

## Deliverables

### Admin Authorization

Implement:

```text
/admin
```

Role requirements:

```text
staff
admin
```

### Dashboard Home

Display:

- Active products
- Orders today
- Revenue
- Low inventory
- Devices in intake
- Needs testing
- Needs IMEI verification
- Open warranties

### Device Intake Module

Create:

```text
/admin/intake
/admin/intake/new
/admin/intake/[id]
```

Features:

- Create intake record (with seller identity section — RCW 19.60 compliance)
- Edit intake record
- Testing checklist
- IMEI verification workflow (record verification service used)
- Upload intake images
- View hold period status and unlock date
- Hold period enforcement (blocked from listing until hold expires)
- Publishing gate status display (shows which requirements are met)
- Convert intake to product (only when all gates pass)
- Reject intake record

### admin_audit_logs

Deploy:

```text
admin_audit_logs
```

Log:
- Product status changes
- Order status changes
- Role changes
- Intake conversions and rejections

### Products Module

Create:

```text
/admin/products
/admin/products/new
/admin/products/[id]
```

Features:

- Create product
- Edit product
- Archive product
- Adjust quantity
- View testing status
- View IMEI verification status
- Edit warranty days
- Edit localized product content

### Product Images

Implement:

- Upload
- Reorder
- Delete
- Primary image
- Localized alt text

### Orders Module

Create:

```text
/admin/orders
/admin/orders/[id]
```

Features:

- View orders
- Update status (with valid transition enforcement)
- Pickup workflow
- Shipping workflow (requires tracking number + carrier input)
- Cancel order (sends cancellation email)
- Review customer cancellation requests from `order_cancellation_requests`

### Warranties Module

Create:

```text
/admin/warranties
/admin/warranties/[id]
```

Features:

- View warranty records
- Search by order number
- Search by customer email
- Search by IMEI
- Search by serial number
- Update claim status
- Update claim notes

### Coupons

Create:

```text
/admin/coupons
```

Features:

- Create coupon
- Edit coupon
- Disable coupon

### Settings

Create:

```text
/admin/settings
```

Features:

- Store info
- Fulfillment settings
- Warranty settings
- Supported locales
- Default locale
- WhatsApp settings
- Localized warranty messaging

## Definition of Done

- Admin dashboard protected (role-checked server-side)
- Product management works (includes network/band compatibility)
- Order management works (tracking number required when shipping)
- Coupons work (atomic redemption)
- Settings save successfully (includes hold period, Stripe Tax, shipping insurance)
- Admin can manage locale-related settings
- Device intake workflow works with seller identity section
- Hold period enforcement works (DB trigger + server gate)
- Testing checklist works
- IMEI verification workflow works (with service name recorded)
- Publishing gate status display works
- Intake-to-product conversion works (only when all gates pass)
- Warranty lookup works
- Warranty claim status can be updated
- Warranty claim emails sent to customers
- Admin audit logs recorded for key actions

---

# Sprint 6 — Launch Readiness

## Goal

Prepare the platform for production.

## Deliverables

### Legal Pages

Create the following pages (required before launch):

```text
/[locale]/legal/terms
/[locale]/legal/privacy
/[locale]/legal/return-policy
/[locale]/legal/warranty-policy
/[locale]/legal/shipping-policy
/[locale]/legal/privacy/data-deletion
```

Requirements:

- Both English and Spanish versions
- Linked from footer on every page
- Privacy Policy includes CCPA language and "Do Not Sell" section
- Data deletion requests are stored in `data_deletion_requests` with a 45-day response deadline
- Return Policy clarifies the in-store purchase policy does NOT apply to customer returns

### SEO

Implement:

- Metadata
- Open Graph
- Canonical URLs
- Structured Data (Product, LocalBusiness)
- hreflang tags for English and Spanish
- Localized metadata

### Sitemap

Create:

```text
/sitemap.xml
```

### Robots

Create:

```text
/robots.txt
```

### Analytics

Implement:

- PostHog
- Vercel Analytics

### Accessibility

Audit:

- Keyboard navigation
- Focus states
- Color contrast
- Screen reader support
- Spanish UI review
- Legal page accessibility

### Performance

Optimize:

- Images
- Bundle size
- Rendering strategy

### Security

Review and verify:

- RLS policies
- Webhook idempotency (`webhook_events` table working)
- Auth security (rate limiting on sign-in/sign-up)
- Secrets (no keys in client code, no exposed SELLER_ID_ENCRYPTION_KEY)
- HTTP security headers verified in production
- `guest_access_token` IDOR protection verified
- Seller ID encryption verified (plaintext never stored)
- Admin audit logs verified

### Compliance Review

Verify:

- Stripe Tax calculating correctly in all U.S. states (test with multiple destination addresses)
- Hold period enforcement working for device intake
- Seller identity fields collected on all intake records
- Legal pages published in English and Spanish
- CCPA "Do Not Sell" link present in footer
- Data deletion request process documented

### Production Deployment

Deploy:

- Production Supabase
- Production Stripe (with Stripe Tax enabled)
- Production Vercel
- Supabase Auth SMTP configured → Resend

## Definition of Done

- Lighthouse ≥95
- Accessibility compliant
- Production deployment successful
- English and Spanish SEO validated
- Legal pages published and linked from footer
- Security review passed
- Compliance review passed
- Stripe Tax verified in production
- Rate limiting active in production

---

# Sprint 7 — Post-MVP ✅ Complete

## Goal

Expand platform capabilities beyond the core MVP.

---

## Repair Workflow ✅

Routes: `/[locale]/repairs` (storefront) + `/admin/repairs` (admin)

Implemented:
- Customer appointment request form
- Admin repair list with status management
- Email notification to admin on new request
- Status: requested → diagnosed → in_progress → completed → cancelled

---

## Trade-In Workflow ✅

Routes: `/[locale]/trade-in` (storefront) + `/admin/trade-ins` (admin)

Implemented:
- Customer preliminary estimate submission
- Disclaimer: online offer is preliminary only
- Admin offer workflow (submitted → under_review → offer_sent → accepted/rejected)
- Email notification to admin on new submission

---

## Product Reviews ✅

Implemented:
- Star ratings on product cards (homepage + shop grid)
- Star summary + review list on product detail page
- `aggregateRating` JSON-LD for Google rich snippets
- Admin moderation queue (`/admin/reviews`) — approve/reject
- `getReviewSummaries()` batch query (no N+1 problem)

---

## Loyalty Program

Status: Not started — Phase 3

---

## Wholesale Portal

Status: Superseded by Sprint 8 Wholesale Batch Intake (below)

---

## Inventory Reservation System

Status: Not started — post-MVP

---

## Supplier Integrations

Status: Not started — post-MVP

---

# Sprint 8 — Operations & Tooling ✅ Complete

## Goal

Improve operational efficiency and store management with tools not in the original spec.

---

## IMEI Scanner Integration ✅

Integration with IMEICheck.com API for real-time device verification at intake.

Features:
- Zebra barcode scanner support (HID keyboard wedge — Enter key triggers lookup)
- Three-service API call strategy (parallel, ~$0.03–0.04/device):
  - Service 11: IMEI to Brand/Model/Name — all brands ($0.01)
  - Service 5: Blacklist Status GSMA — all brands ($0.02)
  - Service 1: Find My iPhone ON/OFF — Apple only ($0.01)
- Auto-fills brand, model, storage, color, serial number, carrier on intake form
- Blacklist badge (Clean / Blacklisted / Unknown) — blacklisted devices still allowed
- FMI (Find My iPhone) warning for Apple devices
- IMEI validation via Luhn checksum (free, local, no API cost)
- `imei_verification_status` and `is_clean_imei` auto-set from API result

Environment variables required:
```
IMEICHECK_API_KEY
IMEICHECK_SERVICE_ID=11
IMEICHECK_BLACKLIST_SERVICE_ID=5
IMEICHECK_APPLE_SERVICE_ID=1
```

---

## Wholesale Batch Intake ✅

Route: `/admin/intakes/wholesale`

For purchasing devices from wholesale suppliers — no RCW 19.60 seller identity required.

Features:
- Supplier info captured once (name, invoice #, date, payment method)
- Per-unit cost default with per-device override
- Zebra scanner integration (auto-refocuses after each scan for back-to-back scanning)
- IMEI Lookup toggle: ON (calls API) / OFF (Luhn local only, free)
- All device fields editable inline (brand, model, storage, condition, notes, cost)
- Duplicate IMEI detection
- Summary bar: device count, total cost, blacklisted count, FMI-on count
- Blacklisted devices allowed (with warning) — intake for parts/review
- Hold period auto-waived for wholesale (RCW 19.60 applies to individual sellers only)
- Single DB insert for all devices

---

## Admin User Management ✅

Route: `/admin/users` (admin-only, not visible to staff)

Features:
- Lists all user profiles with role badges (purple=admin, blue=staff, gray=customer)
- Inline role selector: customer → staff → admin (confirmation dialog)
- Admins cannot change their own role (lockout prevention)

---

## OG Social Images ✅

- Root OG image: `/opengraph-image` (1200×630, dark gradient, brand wordmark)
- Per-product OG image: `/[locale]/product/[slug]/opengraph-image` (1200×630, product photo + specs)
- Driven by `NEXT_PUBLIC_SITE_URL` env var

---

## Footer from Store Settings ✅

Footer is now an async server component that reads from the `settings` table:
- Store name, address, phone, email, hours
- Hours auto-grouped (Mon–Fri, Sat, Sun) and formatted 12h
- Updates immediately when admin changes Settings — no redeploy needed

---

## Domain Migration ✅

- Domain: `wirelessconnectstore.com` (connected to Vercel)
- `NEXT_PUBLIC_SITE_URL=https://wirelessconnectstore.com`
- All email addresses updated to `@wirelessconnectstore.com`
- Resend domain verified for `wirelessconnectstore.com`
- Supabase Auth SMTP → Resend configured

---

## Bug Fixes & Compliance ✅

- **Hold period trigger**: DB trigger now respects `hold_period_waived` flag (migration 018)
- **Double-locale URL bug**: `/en/en/account` fixed across all account pages
- **Google Maps CSP**: `frame-src` updated to allow Google Maps embed on contact page
- **Product reviews**: star ratings on homepage + shop grid cards
- **Real product images**: 31 press photos in Supabase Storage (`product-images/phones/`)
- **LCP optimization**: first product card gets `priority` on `next/image`
- **Stripe live keys**: switched from test to live keys for production
- **Stripe webhook**: registered at `wirelessconnectstore.com/api/stripe/webhook`

---

# 5. Deployment Milestones

## Milestone 1

Foundation Complete

Requirements:

- Sprint 0
- Sprint 1

---

## Milestone 2

Catalog Complete

Requirements:

- Sprint 2

---

## Milestone 3

Commerce Complete

Requirements:

- Sprint 3
- Sprint 4

---

## Milestone 4

Operations Complete

Requirements:

- Sprint 5

---

## Milestone 5

Production Launch

Requirements:

- Sprint 6

---

# 6. Testing Requirements

## Unit Tests

Test:

- Utility functions
- Validation schemas
- Inventory logic
- Order number generation

---

## Integration Tests

Test:

- Product queries
- Cart actions
- Checkout actions
- Stripe integration
- Device intake actions
- IMEI verification actions
- Testing checklist actions
- Warranty record creation

---

## E2E Tests

Use Playwright.

Required flows:

```text
Browse Product
→ Add to Cart
→ Checkout
→ Confirmation
```

```text
Admin Login
→ Create Product
→ Publish Product
```

```text
Admin Login
→ Create Device Intake
→ Complete Testing
→ Complete IMEI Verification
→ Convert to Product
→ Publish Product
```

```text
Admin Login
→ View Order
→ Update Status
```

```text
English Storefront
→ Switch to Spanish
→ Browse Product
→ Add to Cart
→ Checkout
```

```text
Spanish Storefront
→ Place Order
→ Verify Localized Email
```

---

# 7. Release Checklist

Last updated: June 2026

- ✅ Database migrations verified (18 migrations applied, all tables, views, triggers, indexes)
- ✅ Device intake workflow verified (seller identity, hold period, publishing gates)
- ✅ Publishing gate validation verified (15 canonical gates, server-side enforcement)
- ✅ Hold period gate verified — fixed in migration 018 to respect `hold_period_waived`
- ✅ Seller ID encryption verified (plaintext never stored — AES-256-GCM)
- ✅ Webhook idempotency verified (`webhook_events` table, no duplicate processing)
- ✅ Warranty record creation verified (from order item snapshot on Stripe webhook)
- ✅ Warranty claim workflow verified (submitted → under_review → resolved)
- ✅ Stripe production keys configured (live keys on Vercel)
- ✅ Stripe Tax enabled (automatic multi-state sales tax)
- ✅ Stripe webhook registered at `https://wirelessconnectstore.com/api/stripe/webhook`
- ✅ Guest order access token verified (IDOR protection — UUID token required)
- ✅ Admin audit logging verified
- ✅ HTTP security headers verified (CSP, HSTS, X-Frame-Options, etc.)
- ✅ Rate limiting verified (Upstash Redis on auth + coupon endpoints)
- ✅ Legal pages published (Terms, Privacy, Returns, Warranty, Shipping — EN + ES)
- ✅ CCPA "Do Not Sell" link in footer (EN + ES, both locales always shown)
- ✅ Localized email sending verified (EN + ES templates confirmed delivered)
- ✅ Warranty claim email templates verified
- ✅ Order cancellation email verified
- ✅ Password reset email via Supabase Auth SMTP → Resend verified
- ✅ Supabase Auth SMTP → Resend configured and verified
- ✅ Product images: 31 real press photos in Supabase Storage
- ✅ SEO: sitemap, robots, JSON-LD LocalBusiness, per-product aggregateRating
- ✅ OG social images (root + per-product, 1200×630)
- ✅ English and Spanish metadata validated (hreflang, canonical)
- ✅ Analytics (PostHog) configured
- ✅ RLS enabled on all tables
- ✅ Accessibility: WCAG AA color contrast, accessible names, LCP priority image
- ✅ Lighthouse ≥95 (Performance, Accessibility, Best Practices, SEO)
- ✅ Tracking number required when marking orders shipped
- ✅ Domain live at `wirelessconnectstore.com`
- ✅ Footer reads from store settings (dynamic, no hardcoded values)
- ✅ Google Maps embed working on contact page (CSP updated)
- ✅ IMEI scanner operational (IMEICheck.com, $0.03–0.04/check)
- ✅ Wholesale batch intake operational
- ✅ Admin user management operational
- ⏳ Shipping confirmation email (template missing — customer not notified when marked shipped)
- ⏳ Pickup ready email (template missing — customer not notified when ready for pickup)
- ⏳ Welcome email on signup (not implemented)

---

# 8. Definition of MVP Complete

The MVP is complete when:

- Customers can browse products with network compatibility info
- Customers can search products
- Customers can filter products
- Customers can add products to cart
- Customers can purchase products online with automatic state sales tax
- Customers can switch between English and Spanish
- Customers can choose local pickup or nationwide shipping
- Guest customers can view their order via secure token URL
- Customers can submit warranty claims from their account
- Staff can manage products (including network/band compatibility)
- Staff can manage device intake (with seller identity recording and hold period)
- Hold period enforcement prevents premature listing of held devices
- Staff can complete testing and IMEI verification workflows
- Staff can look up warranty records and update claim status
- Warranty records are created from order item snapshots
- Last-unit inventory validation is implemented (atomic SQL)
- Webhook idempotency is enforced (no duplicate processing)
- Staff can manage orders (with tracking number when shipping)
- Localized emails are delivered (all templates in English + Spanish)
- Warranty claim emails are delivered in customer locale
- Legal pages are published and linked from footer
- CCPA compliance elements are in place
- Site is deployed to production with Stripe Tax
- Site meets performance and accessibility targets
- HTTP security headers are configured
- Admin audit logs are recording key actions

---

# 9. Claude Code Execution Instructions

When implementing:

1. Complete one sprint at a time.
2. Do not begin a sprint until the previous sprint passes testing.
3. Use companion documents as the source of truth.
4. Prefer Server Components by default.
5. Use Server Actions for mutations.
6. Use Supabase RLS for data protection.
7. Use Stripe webhooks as payment source of truth.
8. Avoid implementing post-MVP features during MVP development.
9. Maintain deployable code after every sprint.
10. Prioritize simplicity, reliability, and maintainability over feature quantity.
11. Implement localization infrastructure before storefront development.
12. Use next-intl for customer-facing text.
13. Preserve locale when navigating customer-facing routes.
14. Resolve localized content with English fallback.
15. Keep admin dashboard English-only for MVP.
16. Implement device intake before advanced product management.
17. Enforce publishing gates in server actions, not only in UI.
18. Keep intake images private unless explicitly approved for storefront use.
19. Create warranty records from successful Stripe payments.
20. Create warranty records from immutable order item snapshots, not current product values.
21. Implement server-side inventory validation before checkout and after payment confirmation.
22. Timed inventory reservations are post-MVP unless explicitly requested.
